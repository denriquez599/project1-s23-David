import axios from 'axios';

export default async function handler(req, res) {
    let url = 'http://pokeapi.co/api/v2/pokemon/' + randNum;
    try {
        const data = awair axios.get(url);
        
    }
}